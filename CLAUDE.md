# 訪問調整アプリ プロジェクト

## 概要

ケアマネジャーの月間訪問時間の調整を「見える化」するアプリの開発プロジェクト。
壁打ち（ブレインストーミング）をしながら仕様を固め、伴走支援スタイルで開発を進める。

| 項目 | 内容 |
|------|------|
| フォルダ | `C:\Users\kkmh2\claude\訪問調整` |
| 状態 | 初版＋4枠化・種別改修＋**v2.2 機能追加 完了・push済み（2026-08-30）＋v2.2 レビュー指摘の修正（2026-08-31・テスト41件PASS・未push）**。**GitHub Pages 公開済み（2026-07-03）** |
| 形式 | 案A確定：単一HTML + localStorage（`visit-planner.html`） |
| 設計書 | 初版: `docs/superpowers/specs/2026-07-02-visit-planner-design.md`（v2）／改修: `2026-07-03-visit-planner-4slots-types-design.md` |
| リポジトリ | `https://github.com/cmkurashikibranch/visit-planner`（パブリック・masterブランチ） |
| 公開URL | `https://cmkurashikibranch.github.io/visit-planner/` |
| エントリ | `index.html`（`visit-planner.html` のコピー。**2ファイル同期が必要**・他アプリと同じ運用） |

## 今後の更新手順

```powershell
cd "C:\Users\kkmh2\claude\訪問調整"
copy visit-planner.html index.html
git add -A
git commit -m "update: 変更内容"
git push
```

数分後に公開URLへ反映される。データは各端末のブラウザ内（localStorage）に保存されるため、公開後もデータが共有されるわけではない点に注意（PCで入れたデータはPCのみ。共有したければJSONエクスポート/インポート）。

## コンセプト（ユーザーのイメージ）

**「月間カレンダーに、利用者の名前が書かれたピースをはめ込んでいく」**

```
未配置プール [山田さん][佐藤さん]… ──D&D──▶ 月間カレンダーの訪問枠①②③
プールが空になったら今月の調整完了。残っているピース＝未調整の人
```

## 壁打ちで確定した要件（2026-07-02）

| 項目 | 決定内容 |
|------|---------|
| 解決したい課題 | 抜け漏れ・偏り・空き枠把握・利用者都合との調整（全部） |
| 利用者数 | 20〜40名程度（標準的な担当件数） |
| 位置づけ | **独立した専用アプリ**。完成後にGoogleカレンダー連携を検討（2段構え） |
| ピースの情報 | 名前＋訪問の都合メモ（曜日・時間帯NG）＋訪問時刻＋訪問済みチェック（地区情報は不要） |
| 月替わり | 「前月コピー」「まっさら」を選べる |
| 使用端末 | 主にPC（D&DはPC専用、スマホは閲覧のみ）— schedule_v4と同方式 |
| 1日の枠 | **枠制＋時刻調整**：訪問①②③の3枠（現行Googleカレンダー運用と同じ）、枠内で時刻微調整可 |
| 重要フロー | **訪問済みチェック → その場で来月の予約を入れる**（当月と来月を行き来する運用。来月予約済みの人は月替わり時すでに配置済み扱い） |

## 実現方式（3案提示済み・案A推奨）

- **案A（推奨）**：単一HTML + localStorage。schedule_v4・お客様ノートと同じ実績方式。D&D実装ノウハウ流用可
- 案B：最初からFirebase → 初期設定が重く見送り推奨
- 案C：schedule_v4に統合 → 「独立アプリ」の決定により見送り

**設計方針**：データ構造は日付＋時刻を持つ形にして、将来のGoogleカレンダー連携（イベント変換）をしやすくする。

## 画面イメージ（提示済み・承認待ち）

- ヘッダー：月ナビ＋「未配置: N名 / 全体M名」カウンター
- 左：未配置プール（ピースに都合メモ表示。例：⚠火曜NG）
- 右：月間カレンダー（各日に訪問①②③の3枠、✓=訪問済み、空欄=空き枠）
- 訪問済みチェック時に「来月の予約を入れますか？」→来月カレンダーへ配置
- 利用者マスタ（名前・都合メモ）は設定画面で管理

## 次にやること（再開時）

**初版（Task 1〜9）＋4枠化・種別改修（2026-07-03）＋v2.2 機能追加（2026-08-30）まで完了。**

- 進捗台帳＝`.superpowers/sdd/progress.md`（初版＋改修の全記録あり）
- **次＝v2.2 レビュー修正のpush（オーナー確認待ち）。**レビューは2026-08-31に実施済み（Critical 0／Important 2／Minor 4）
- **その次＝v2.1磨き込み改修の実装**（2026-07-06 spec・実装計画作成済み。v2.2 とは別件で未着手）
  - spec: `docs/superpowers/specs/2026-07-06-visit-planner-polish-design.md`（spec-reviewer承認済み）
  - 実装計画: `docs/superpowers/plans/2026-07-06-visit-planner-polish.md`（全5タスク・全コード付き）
  - 内容: 改善候補4件＋既存クラッシュバグ1件（予約モード中の対象者削除/インポート）。公開pushはオーナー確認後

## v2.2 機能追加（2026-08-30・完了・push済み）

オーナー依頼の4点。着手前に AskUserQuestion で3点（休みの日の枠の扱い／日曜データの行き先／ふりがな欄の要否）を確認し、いずれも推奨案が選ばれた。

1. **休日（休み／AM休／PM休）**：日付ヘッダ `.day-head` クリック → ポップオーバーで設定・解除。`appData.holidays = { "YYYY-MM-DD": "full"|"am"|"pm" }`。full=4枠／am=午前①②／pm=午後①②が `.cal-slot.blocked`（▨ 休み）になり**配置不可**（クリック・D&D・選択モード・予約モード・前月コピーの全経路で遮断）。予定が入っている日を休みにすると確認ダイアログ→プールへ戻す。日ヘッダの残枠表示は `n/4` → `n/使える枠数`（full のときは非表示）
2. **種別の色**：会議＝**赤 `#D93025`**、その他＝**紺 `#3B50A0`**（旧・会議色を回した。旧・その他のグレー `#6B7280` は廃止）
3. **日曜の欄を廃止**：カレンダーは月〜土の**6列**。先頭空きセルは `dw - 1`（日曜は `continue` で飛ばす）。既存の日曜配置は `sanitizeData` が落として `sundayPooled` に計上→バナー通知。`copyFromPrevMonth` もコピー先が日曜・休み枠ならプール行き。**副作用：今日が日曜だと `today` ハイライトが出ない**（仕様）
4. **五十音順**：利用者に `kana`（ふりがな・任意）を追加。`compareUsers` ＝ ふりがな有りが先（`kanaKey` でカタカナ→ひらがな・空白除去して比較）→ 無い人は名前順で後ろ。プール／配置ポップオーバー／利用者管理の3か所に適用。プールの検索もふりがなで引ける
5. 併せて **未配置プールを2列**（`#pool-list` を grid 2列・`main` の左カラム 200px→340px）、モバイルの `.cal-grid` min-width 560→540px

- **データ version 3**：`{ version, users[{id,name,kana,ngDays,memo}], placements, holidays, initializedMonths, nextId }`。v1・v2 は `sanitizeData` が自動移行（localStorage キーは `visit-planner-v1` のまま）
- **アンドゥは holidays も含む**（`snapshotUndo` が `{placements, holidays}` を保存）。`resetCurrentMonth` は holidays を消さない（配置ではなく本人の休みのため）
- 検証：`node --test tests/core.test.js` **39/39 PASS**（新規16件）＋実ブラウザ（Playwright）で休日の設定/解除/確認ダイアログ/アンドゥ・休み枠の遮断4経路・6列レイアウト・プール2列・チップ色・ふりがな順・日曜データのプール戻し・前月コピー（日曜/休みでプール行き）・**来月が全日休みでも予約モードから抜けられること**を確認。コンソールエラー0
- 先頭空きセル計算は node で **2024〜2030の全月2192日**を総当たりして列ズレ0を確認（月初が日曜・月末が日曜のケース含む）
- コミット `f00c20a`（push済み・公開URLへ反映済み）
- ⚠️ **code-reviewer エージェントは2回とも600秒 stall で失敗**（2026-08-30・08-31）。差分をファイルに落として範囲を絞っても再現。**このプロジェクトのレビューは司令塔側で読むほうが早い**
- ⚠️ **`py -c "..."` を Bash ツール経由で叩くと、二重引用符の中のバッククォートがコマンド置換として実行される**（2026-08-30に本ファイルを壊した）。日本語＋バッククォートを含む長文を書き戻すときは、スクリプトを**ファイルに書いてから実行**する

## v2.2 コードレビュー（2026-08-31・司令塔で実施）

Critical 0／Important 2／Minor 4。**修正したのは2件**（オーナー選択）。

**修正済み**

- **Important 1：休日設定で外れる予定の文言が実態と違っていた。**`placementsBlockedBy` は種別を問わず返すが、**プールはモニタ未配置の人しか並べない**（`renderPool` の `hasMonitoringInMonth` フィルタ）ため、会議・他は戻る先が無く消える。「N件をプールへ戻します」は嘘だった。純関数 `conflictSummary(conflicts, past)` を CORE に追加し、ダイアログ＝未来形／トースト＝完了形で内訳を出す形へ
- **Minor 1：**`sanitizeData` の holidays 検証が**1要素の配列 `['full']` を素通ししていた**（`HOLIDAY_KINDS[['full']]` が文字列強制で引けてしまう）。落ちはしないが以後 `k === cur` の厳密比較が全部外れる。`typeof === 'string'` を追加

**未修正（記録のみ）**

- **Important 2：起動時の `sanitizeData`（日曜・休みの枠）で消える会議・他はアンドゥが効かない。**バナーの「プールへ戻しました」も同じ嘘。⚠️ ただし `loadData` は `saveData()` を呼ばないので、**次の操作までは localStorage に原本が残る**。実機に日曜の予定が実在するか確認してから触る
- Minor 2：`kanaKey` が半角カナを変換しない（並びの帯が分かれ、ひらがな検索で引けない）
- Minor 3：予約モード・選択モード中の日ヘッダクリックが無反応（フィードバック無し）
- Minor 4：`tryPlace` の `isSunday` ガードはUIから到達不能。二重防御として妥当だがメッセージが理由と食い違う

**問題なしと確認した項目**：休み枠の遮断は実質7経路すべて塞がっている／`compareUsers` は推移律・対称性の違反0（6要素216通りの総当たり）／不正な holidays（null・配列・文字列・値null）で落ちない／アンドゥは placements・holidays とも復元／エクスポートは holidays を含む／`sanitizeData` の処理順（holidays→placements）が正しい

## 4枠化＋種別改修（2026-07-03・完了）

- **1日4枠**：午前①10:00／午前②11:00／午後①14:00／午後②15:30（配置後に時刻手入力変更可）
- **種別**：モニタ（紫 `--type-mon`）／会議（紺 `--type-meet`）／他（グレー `--type-other`）。ピースは色帯4px＋チップ表示 ※**色は v2.2 で変更済み**（会議＝赤／他＝紺）。以下はこの時点の記録
- **プール＝今月モニタ未配置の人**。プールからの配置は常にモニタ。会議・他は「空き枠クリック→配置ポップオーバー」（全員selectから選択・種別3ボタン）
- **モニタは月1件まで**（2件目は showInfoToast で拒否）。会議・他は制限なし。✓の来月予約フローはモニタのみ
- **データ v2**：placement に `type` 追加。v1データは sanitizeData が自動移行（旧①→午後①・旧②→午後②・旧③はプールへ戻し `migratedPooled` でバナー通知）。localStorage キーは `visit-planner-v1` のまま
- 実装コミット：`2595af0`（CORE・TDD 24テスト）→ `2a76321`（描画・CSS）→ `66d6dae`（配置ポップオーバー）→ `4028d6e`（詳細種別切替・✓分岐）
- 検証：`node --test tests/core.test.js` 24/24 PASS＋実ブラウザ注入テスト30アサーションPASS＋390pxスクショ確認（受け入れ9項目すべてPASS）
- 検証の学び：Playwright evaluate はクラッシュするため「サーバーでスクリプト注入→document.title で結果回収」方式が安定

## 実装進捗（2026-07-03時点）

| タスク | 状態 | コミット |
|--------|------|---------|
| Task 1: HTML骨格＋CORE判定系純関数（TDD） | ✅ レビュー承認 | `6073ebe` |
| Task 2: CORE変更系純関数（TDD・16テスト） | ✅ レビュー承認 | `fe2b18f`＋`bff2b1f` |
| Task 3: 画面骨格・CSS・ストレージ・描画 | ✅ レビュー承認 | `e975e68` |
| Task 4: 利用者管理CRUD＋JSONエクスポート/インポート | ✅ レビュー承認 | `e71205f`＋`b2be232` |
| Task 5: クリック配置・詳細ポップオーバー・アンドゥ | ✅ レビュー承認 | `a9156d5`＋`02d12a9` |
| Task 6: D&D（PC専用）＋NG事前ハイライト＋pendingDrop | ✅ レビュー承認 | `29f6d48`＋`ffa50c9` |
| Task 7: ✓トグル＋来月予約フロー（3択モーダル） | ✅ レビュー承認 | `5b8aad7` |
| Task 8: 月替わりダイアログ＋今月リセット | ✅ レビュー承認 | `84bc219` |
| Task 9: スマホ対応＋総合動作確認 | ✅ レビュー承認・全10項目PASS | `d709acb`＋`4bbb34f` |
| 最終ホールブランチレビュー＋fix | ✅ 完了（Critical 0・Important 1→修正済み） | `b63f215` |

- テスト：`node --test tests/core.test.js` → 16/16 PASS
- レビューでのMinor指摘（最終レビューで棚卸し）：module.exports残骸／sanitizeDataのID重複未排除／tryPlace・tryMoveの早期returnがrenderAll非呼出（実害なし）／予約モード中の月ナビ移動が可能（仕様外メモ）／resetCurrentMonthのrenderAll二重呼び（実害なし）

## 追加確定事項（2026-07-02 設計フェーズ）

- 訪問頻度は全員月1回（1人＝1ピース）／土日も配置可の7列カレンダー／NG曜日は警告付き
- スマホは✓チェック・クリック配置・来月予約まで可（D&DのみPC専用）
- UI/UX＋シニアエンジニアの2体レビュー計24件を設計書v2に反映（月替わりは initializedMonths フラグ、重複ガード isPlacedInMonth 一元化、クリック配置の代替操作、予約モードバナー＋自動復帰、直前1操作アンドゥなど）

## 関連プロジェクト

- スケジュール管理アプリ（schedule_v4.html）：`../スケジュール管理/` — 月間カレンダーD&Dの実装実績あり（スキル⑦）
- Googleカレンダー運用：ルート CLAUDE.md スキル①参照（訪問①13:00〜/②14:30〜/③16:00〜の3枠運用）

## 進捗

| 日付 | 内容 |
|------|------|
| 2026-07-02 | プロジェクト開始・壁打ち完了（要件確定）・方式3案＋画面イメージ提示。承認待ちで中断 |
| 2026-07-03 | Task 9完了（レスポンシブCSS＋設計書§9全10項目のブラウザ実表示確認PASS）。最終ホールブランチレビュー実施→Important 1件（予約モード×選択モード衝突）を修正。**全実装完了・公開待ち** |
| 2026-08-30 | v2.2 機能追加（休日入力・会議=赤/他=紺・日曜廃止で6列＋プール2列・ふりがな五十音順）完了。テスト39件PASS・実ブラウザ検証済み・push済み |
| 2026-08-31 | v2.2 のコードレビュー実施（司令塔側で通読）。Critical 0／Important 2／Minor 4。うち2件を修正・テスト41件PASS |

---

*最終更新：2026-08-31*
